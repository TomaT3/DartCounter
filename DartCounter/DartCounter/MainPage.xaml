<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DartCounter"
             xmlns:viewmodel="clr-namespace:DartCounter.ViewModel"
             x:Class="DartCounter.MainPage">

    <StackLayout>
        <Button 
            Text="START"
            IsEnabled="{Binding ButtonEnabled}"
            Command="{Binding ButtonSwitchToSettingsViewCommand}"/>
    </StackLayout>

</ContentPage>
